# Preset and macro definition editor

To start dev environment, install js dependencies `npm i` then run `npm run dev` and open a browser to `http://localhost:5173/preseteditor.html` to see the preset editor ui

To build for device deployment run `npm run bundle` which bundles and copies the output into the `../sdcard_image/www` folder
